/**
 * Tier 0 scrape — the live, logged-in DOM. JSON-LD Product first, then
 * visible-price heuristics. Result rides along with the create request and the
 * item arrives `rich`, skipping the server ladder entirely.
 * Spec: 03-ARCHITECTURE.md §4.2 Tier 0 · P1 task 16 — not yet written.
 */
export {};
